#ifndef _EXTERN_H_
#define _EXTERN_H_

// The base's coordinates 'base_x' and 'base_y' are global and defined in the
// main function. Therefore, they're defined externally and are handled by the
// extern library, which contains all extern variable declarations.

extern int base_x;
extern int base_y;

#endif